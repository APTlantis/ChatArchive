# Changelog

All notable product changes are recorded here. Store publication status is recorded separately in release notes and never inferred from this file.

## 0.2.0 — Export Continuity (candidate)

- Adds Library refresh and restore controls for newer OpenAI export ZIP files.
- Retains user-authored library state for stable identifiers and marks missing export targets unavailable instead of deleting that state.
- Maintains one coherent rollback snapshot containing the prior derived archive, SQLite state, and machine-readable snapshot metadata.
- Adds distribution-grade release, migration, security, provenance, SBOM, and withdrawal records.

## 0.1.2 — historical public release

- Microsoft Store listing is the authoritative public-status evidence. Historical local MSIX, WACK, and lifecycle records remain historical evidence only.
